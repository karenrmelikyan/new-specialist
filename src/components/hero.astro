---
import Time from '../components/Time.astro'
---

<style>
    .hero {
        background: linear-gradient(180deg,#fbfdff 0%, #ffffff 60%);
    }

    .hero-title {
        font-size: var(--huge);
        color: var(--main-font-color);
        font-weight: var(--font-weight);
        line-height: 1.25;
        margin: 0;
    }

    .hero-sub {
        display: flex;
        flex-direction: column;
        gap: 5px;
        padding: 0 20px;
        margin: 0;
    }
</style>

<section class="hero">
    <div class="container hero-grid">
        <h1 class="hero-title">Specialist: Сервисная служба</h1>
        <div class="offset"></div>
        <ul class="hero-sub" aria-label="Преимущества сервиса">
            <li>Обслуживаем технику с 2002 года.</li>
            <li>Бытовые услуги на дому.</li>
            <li>Гарантия до 12 месяцев.</li>
        </ul>
        <div class="offset"></div>
        <div class="inline-group">
            <button class="btn-large">
                Вызвать мастера
            </button>
            <Time label="Прием заявок: 24/7" />
        </div>
    </div>
</section>